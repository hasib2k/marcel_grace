import SectionHeader from "./SectionHeader";

const OurClients = () => {
  return (
    <div className="py-10 bg-[#DCEBFF]">
      <SectionHeader
        title="Our Clients"
        subtitle="We are honored to have worked with some of the world's leading brands."
      />
    </div>
  );
};

export default OurClients;
